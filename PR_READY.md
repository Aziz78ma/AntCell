This pull request bundle was prepared by the assistant.

Changes include:
- Cinematic viewport with stylized panda (toon shading + local GLB fallback)
- Optional postprocessing composer (Bloom, Chromatic Aberration, Noise, DOF)
- UI refinements: scanlines, film-grain, auto-orbit toggle

If you see this file it was added to create a small unique commit so a PR can be opened.
